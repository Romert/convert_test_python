#!/bin/bash

# Цвета
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;36m'
NC='\033[0m'

echo -e "${GREEN}🔍 Управление старыми файлами и папками (по дням, с выбором глубины)${NC}"
echo

# 1. Каталог поиска
while true; do
    read -p "📁 Введите путь к каталогу для поиска (по умолчанию: текущая директория): " target_dir
    target_dir="${target_dir:-.}"
    if [ -d "$target_dir" ]; then
        echo -e "${GREEN}✅ Каталог '$target_dir' найден.${NC}"
        break
    else
        echo -e "${RED}❌ Каталог не существует. Попробуйте снова.${NC}"
    fi
done

# 2. Глубина поиска
echo
echo "🕳️ Укажите глубину поиска (0 = только сам каталог, 1 = каталог + подкаталоги 1 уровня и т.д.):"
echo "   Примеры:"
echo "     • 0 — не заходить в подкаталоги (только содержимое указанной папки)"
echo "     • 1 — включая подкаталоги первого уровня"
echo "     • Пусто или -1 — без ограничения (по умолчанию)"
while true; do
    read -p "Введите глубину (по умолчанию: без ограничения): " depth_input
    depth_input="${depth_input:--1}"  # -1 = без ограничения

    if [[ "$depth_input" == "-1" ]] || [[ -z "$depth_input" ]]; then
        maxdepth_flag=""
        depth_desc="без ограничения"
        break
    elif [[ "$depth_input" =~ ^[0-9]+$ ]]; then
        maxdepth_flag="-maxdepth $depth_input"
        depth_desc="глубина $depth_input"
        break
    else
        echo -e "${RED}❌ Глубина должна быть целым неотрицательным числом или оставлена пустой.${NC}"
    fi
done

# 3. Тип объектов
echo
echo "🔎 Что искать?"
select choice in "Только файлы" "Только папки" "Файлы и папки"; do
    case $REPLY in
        1) type_flag="-type f"; break ;;
        2) type_flag="-type d"; break ;;
        3) type_flag=""; break ;;
        *) echo -e "${RED}Неверный выбор. Повторите.${NC}" ;;
    esac
done

# 4. Возраст в днях
echo
read -p "📅 Введите возраст в днях (по умолчанию: 60): " days
days="${days:-60}"

if ! [[ "$days" =~ ^[0-9]+$ ]] || [ "$days" -lt 0 ]; then
    echo -e "${RED}⚠️ Некорректное значение. Используем 60 дней.${NC}"
    days=60
fi

# 5. Поиск
echo -e "${YELLOW}Выполняется поиск (глубина: $depth_desc) объектов, изменённых более $days дней назад...${NC}"
results_file=$(mktemp -t old_files.XXXXXX)

# Формируем и выполняем команду find
find "$target_dir" $maxdepth_flag $type_flag -mtime +$days > "$results_file"

if [ ! -s "$results_file" ]; then
    echo -e "${YELLOW}⚠️ Ничего не найдено.${NC}"
    rm -f "$results_file"
    exit 0
fi

count=$(wc -l < "$results_file")
echo -e "${GREEN}✅ Найдено $count объектов.${NC}"

# 6. Сохранить список?
echo
read -p "📄 Сохранить список найденных объектов в файл? (y/n, по умолчанию: y): " save_list
save_list="${save_list:-y}"
if [[ "$save_list" =~ ^[Yy]$ ]]; then
    default_list="old_files_$(date +%Y%m%d_%H%M%S).txt"
    read -p "   Имя файла (по умолчанию: $default_list): " list_name
    list_name="${list_name:-$default_list}"
    cp "$results_file" "$list_name"
    echo -e "${GREEN}✅ Список сохранён: $list_name${NC}"
fi

# 7. Действие
echo
echo "🛠️ Что сделать с найденными объектами?"
echo "1) Ничего (только показать)"
echo "2) Архивировать в .tar.gz"
echo "3) Удалить (осторожно!)"
while true; do
    read -p "Выберите действие (1/2/3): " action
    case $action in
        1)
            echo -e "${BLUE}ℹ️ Никаких изменений не будет выполнено.${NC}"
            break
            ;;
        2)
            default_arc="old_archive_$(date +%Y%m%d_%H%M%S).tar.gz"
            read -p "   Имя архива (по умолчанию: $default_arc): " arc_name
            arc_name="${arc_name:-$default_arc}"

            echo -e "${YELLOW}Создаётся архив...${NC}"
            if tar -czf "$arc_name" -T "$results_file" 2>/dev/null; then
                echo -e "${GREEN}✅ Архив создан: $arc_name${NC}"
            else
                echo -e "${RED}❌ Ошибка при создании архива. Возможно, слишком много файлов или нет прав.${NC}"
            fi
            break
            ;;
        3)
            # === УЛУЧШЕННОЕ ПОДТВЕРЖДЕНИЕ УДАЛЕНИЯ ===
            echo
            echo -e "${RED}⚠️ ВНИМАНИЕ: Вы собираетесь УДАЛИТЬ $count объектов!${NC}"
            echo -e "${RED}Это действие НЕОБРАТИМО.${NC}"
            
            echo -e "${YELLOW}Примеры объектов для удаления:${NC}"
            head -n 5 "$results_file" | while IFS= read -r line; do
                echo "  • $line"
            done
            if [ "$count" -gt 5 ]; then
                echo "  ... и ещё $((count - 5)) объектов"
            fi
            echo

            echo -e "${RED}Чтобы подтвердить удаление, введите ТОЧНО: ${BLUE}YES${NC}"
            read -p "Ваш ввод: " confirm
            if [[ "$confirm" == "YES" ]]; then
                echo -e "${YELLOW}Выполняется удаление...${NC}"
                while IFS= read -r item; do
                    if [ -e "$item" ]; then
                        rm -rf "$item"
                    fi
                done < "$results_file"
                echo -e "${GREEN}✅ Успешно удалено $count объектов.${NC}"
            else
                echo -e "${BLUE}❌ Удаление отменено. Ничего не изменено.${NC}"
            fi
            break
            ;;
        *)
            echo -e "${RED}Неверный выбор. Введите 1, 2 или 3.${NC}"
            ;;
    esac
done

# Очистка
rm -f "$results_file"

echo
echo -e "${GREEN}Готово!${NC}"
